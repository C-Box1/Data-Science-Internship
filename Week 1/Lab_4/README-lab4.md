# Lab 4 — Lists, Tuples, and Dicts

## Overview
Focused on Python's core data structures: lists (mutable sequences), tuples (immutable sequences), and dictionaries (mappings). Also covered list comprehensions and slicing.

---

## Checklist

### Lists & slicing
- [ ] Exercise 1 — Implemented `every_third(l)` using slices and `first_and_last(l)` with unpacking

### Tuples
- [ ] Exercise 2 — Implemented `gcd(a, b)` using tuple packing/unpacking (Euclidean algorithm)

### Dictionaries
- [ ] Exercise 3 — Implemented `flip_dict(input_dict)` — reverses keys and values, collecting duplicate values into lists
- [ ] Exercise 4 — Implemented `merge_keys(input_dict)` — merges keys with the same value into a tuple key

### Comprehensions
- [ ] Read section — Predicted outputs of provided list/set comprehensions
- [ ] Exercise 5 — Wrote comprehensions for 6 data transformations (double+add, capitalize, filter, TA names, word-length tuples, word-length dict)
- [ ] Exercise 6 — Implemented `generate_pascal_row(row)` using `zip`
- [ ] Exercise 7 — Implemented `print_pascal_triangle(n)` with centered formatting

### Bonus
- [ ] Exercise 8 — Implemented `polygon_collision(poly1, poly2)` (hard — optional)

---

## Difficulties
> *Fill in what you found hard or confusing during this lab.*

-

---

## Feedback
> *Your thoughts on this lab — what worked, what didn't, what surprised you.*

-
