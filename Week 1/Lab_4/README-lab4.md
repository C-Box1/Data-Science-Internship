# Lab 4 — Lists, Tuples, and Dicts

## Overview

Focused on Python's core data structures: lists (mutable sequences), tuples (immutable sequences), and dictionaries (mappings). Also covered list comprehensions and slicing.

---

## Checklist

### Lists & slicing

- [X] Exercise 1 — Implemented `every_third(l)` using slices and `first_and_last(l)` with unpacking

### Tuples

- [X] Exercise 2 — Implemented `gcd(a, b)` using tuple packing/unpacking (Euclidean algorithm)

### Dictionaries

- [X] Exercise 3 — Implemented `flip_dict(input_dict)` — reverses keys and values, collecting duplicate values into lists
- [X] Exercise 4 — Implemented `merge_keys(input_dict)` — merges keys with the same value into a tuple key

### Comprehensions

- [X] Read section — Predicted outputs of provided list/set comprehensions
- [X] Exercise 5 — Wrote comprehensions for 6 data transformations (double+add, capitalize, filter, TA names, word-length tuples, word-length dict)
- [X] Exercise 6 — Implemented `generate_pascal_row(row)` using `zip`
- [X] Exercise 7 — Implemented `print_pascal_triangle(n)` with centered formatting

### Bonus

- [ ] Exercise 8 — Implemented `polygon_collision(poly1, poly2)` (hard — optional)

---

## Difficulties

- In this Lab I faced the most difficulty in the dictionaries section when trying to fix the flip_dict() function so that it can work when doing merging and flipping multiple times and then had difficulty trying to write the pretty pascal triiangle when trying to fill the spaces as for the bonus I figured one way of considered intersection.

---

## Feedback

- This Lab was pretty challenging i tried multiple ways of working with lists tuples and dictionaries but overall it was a very helpful lab in understanding how the different data structures in Python.
