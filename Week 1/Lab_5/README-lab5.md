# Lab 5 — Functions

## Overview
In-depth exploration of Python functions: positional and keyword arguments, default values, variadic parameters (`*args`, `**kwargs`), scope, mutable default arguments, and documentation conventions.

---

## Checklist

### Exploring arguments and parameters
- [ ] Exercise 1 — Identified valid/invalid calls for `print_two(a, b)` and commented out invalid ones
- [ ] Exercise 2 — Identified valid/invalid calls for `keyword_args(a, b=1, c='X', d=None)`
- [ ] Exercise 3 — Identified valid/invalid calls for `variadic(*args, **kwargs)`
- [ ] Exercise 4 — Identified valid/invalid calls for `all_together(x, y, z=1, *nums, indent=True, spaces=4, **options)`

### Writing functions
- [ ] Exercise 5 — Implemented `speak_excitedly(msg, n_excl_marks=1, in_caps=False)` and wrote the four required calls
- [ ] Exercise 6 — Implemented `average(*args)` returning the mean or `None` for no arguments, and called it with an unpacked list
- [ ] Exercise 7 — Implemented `make_table(**kwargs)` with `key_justify` and `value_justify` alignment options (bonus)

### Review exercises
- [ ] Review 1 — Predicted output of `return` vs `print` examples
- [ ] Review 2 — Understood `reassign` vs `append_one` (object reference vs rebinding)
- [ ] Review 3 — Traced variable scope across two `foo()` case studies
- [ ] Review 4 — Understood the `UnboundLocalError` and why it occurs
- [ ] Review 5 — Understood mutable default argument behavior (`append_twice`) and the `None` sentinel fix
- [ ] Review 6 — Read and understood `__doc__` docstrings
- [ ] Review 7 — Read and understood parameter annotations (`__annotations__`)
- [ ] Exercise 8 — Implemented `autodoc(f)` that appends argument and return type info to a function's docstring

---

## Difficulties
> *Fill in what you found hard or confusing during this lab.*

-

---

## Feedback
> *Your thoughts on this lab — what worked, what didn't, what surprised you.*

-
